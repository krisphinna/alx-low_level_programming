#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char low_case;

	for (low_case = 'a'; low_case <= 'z'; low_case++)
		putchar(low_case);
	
		return (0);
}
