#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char low_case;

	while (low_case = 'a' && <= 'z')
		putchar(low_case++);
	purchar('\n');
	return (0);
}
